plugins {
    id 'multiloader-loader'
    id 'net.fabricmc.fabric-loom'
}

def identifier = "${mod_id}-${minecraft_version}-fabric"

dependencies {
    minecraft "com.mojang:minecraft:${minecraft_version}"
    implementation "net.fabricmc:fabric-loader:${fabric_loader_version}"
    implementation "net.fabricmc.fabric-api:fabric-api:${fabric_version}"

    implementation group: 'com.github.wendall911.climatesettings', name: "climatesettings-${minecraft_version}-fabric", version: "${climatesettings_version}"
    include group: 'com.github.wendall911.climatesettings', name: "climatesettings-${minecraft_version}-fabric", version: "${climatesettings_version}"

    implementation group: 'technology.roughness.whitenoise', name: "whitenoise-${minecraft_version}-fabric", version: "${whitenoise_version}"
    include group: 'technology.roughness.whitenoise', name: "whitenoise-${minecraft_version}-fabric", version: "${whitenoise_version}"

    implementation group: 'technology.roughness.handbook', name: "handbook-${minecraft_version}-fabric", version: "${handbook_version}"
    include group: 'technology.roughness.handbook', name: "handbook-${minecraft_version}-fabric", version: "${handbook_version}"

    implementation group: 'com.github.wendall911.homeostaticseasons', name: "homeostaticseasons-${minecraft_version}-fabric", version: "${homeostaticseasons_version}"

    //localRuntime "dev.architectury:architectury-fabric:${architectury_version}"
    /*
    localRuntime("me.shedaniel.cloth:cloth-config-fabric:${cloth_config_version}") {
        exclude(group: "net.fabricmc.fabric-api")
    }
    */
    compileOnly "me.shedaniel:RoughlyEnoughItems-api-fabric:${rei_version}"
    compileOnly "me.shedaniel:RoughlyEnoughItems-default-plugin-fabric:${rei_version}"
    /*
     * TODO Re-enable REI when it is released
    localRuntime "me.shedaniel:RoughlyEnoughItems-fabric:${rei_version}"
    */

    compileOnly "mezz.jei:jei-${jei_minecraft_version}-common-api:${jei_version}"
    compileOnly "mezz.jei:jei-${jei_minecraft_version}-fabric:${jei_version}"
    //compileOnly "curse.maven:create_fabric-624165:${create_fabric_version}"
    implementation "org.ladysnake.cardinal-components-api:cardinal-components-base:${cardinal_components_version}"
    implementation "org.ladysnake.cardinal-components-api:cardinal-components-entity:${cardinal_components_version}"
    include "org.ladysnake.cardinal-components-api:cardinal-components-base:${cardinal_components_version}"
    include "org.ladysnake.cardinal-components-api:cardinal-components-entity:${cardinal_components_version}"
    compileOnly "maven.modrinth:serene-seasons:${serene_seasons_fabric_version}"
    compileOnly "maven.modrinth:glitchcore:${glitch_core_fabric_version}"

    localRuntime "net.jodah:typetools:0.6.3"
    implementation "com.electronwill.night-config:core:3.6.7"

    localRuntime group: 'com.terraformersmc', name: 'modmenu', version: "${mod_menu_version}"

    if (System.getenv('DATA_GEN') != null) {
        // Datagen mods
    }
    else {
        // Runtime testing mods
        localRuntime "mezz.jei:jei-${jei_minecraft_version}-fabric:${jei_version}"
        // This doesn't work for whatever reason. Such a strange port
        //localRuntime "curse.maven:create_fabric-624165:${create_fabric_version}"
        //localRuntime "curse.maven:glitchcore-955399:${glitch_core_fabric_version}"
        //localRuntime "curse.maven:biomesoplenty-220318:${bop_fabric_version}"
        //localRuntime "curse.maven:terrablender-565956:${terrablender_fabric_version}"
        //localRuntime "curse.maven:sereneseasons-291874:${serene_seasons_fabric_version}"
        //localRuntime "curse.maven:regions-659110:${regions_fabric_version}"
        //localRuntime "curse.maven:simpletextoverlay-321283:${simpletextoverlay_fabric_version}"
        //localRuntime "curse.maven:more_food-874343:${more_food_fabric_version}"
        //localRuntime "curse.maven:colorful_hearts-854213:${colorful_hearts_fabric_version}"
        //localRuntime "curse.maven:puzzles_lib-495476:${puzzles_lib_fabric_version}"
        //localRuntime "curse.maven:forge_config_api_port-547434:${forge_config_api_fabric_version}"
        //localRuntime "curse.maven:lmba-281715:${lmba_fabric_version}"
        //localRuntime "curse.maven:byg-1070751:${byg_fabric_version}"
        //localRuntime "curse.maven:tyg-962544:${tyg_fabric_version}"
        //localRuntime "curse.maven:corgilib-693313:${corgilib_fabric_version}"
        //localRuntime "software.bernie.geckolib:geckolib-fabric-${minecraft_version}:${geckolib_fabric_version}"
        // Bakery and Beach crash in a dev environment.
        // Let's NOT Do architectury please
        //localRuntime "curse.maven:letsdo_bakery-1038130:${letsdo_bakery_fabric_version}"
        //localRuntime "curse.maven:letsdo_beach-858691:${letsdo_beach_fabric_version}"
        //localRuntime "curse.maven:letsdo_blooming-962885:${letsdo_blooming_fabric_version}"
        //localRuntime "curse.maven:letsdo_brewery-1038106:${letsdo_brewery_fabric_version}"
        //localRuntime "curse.maven:letsdo_candlelight-1038117:${letsdo_candlelight_fabric_version}"
        //localRuntime "curse.maven:letsdo_herbal-951221:${letsdo_herbal_fabric_version}"
        //localRuntime "curse.maven:letsdo_meadow-821483:${letsdo_meadow_fabric_version}"
        //localRuntime "curse.maven:letsdo_farm-1038103:${letsdo_farm_fabric_version}"
        //localRuntime "curse.maven:letsdo_vinery-704465:${letsdo_vinery_fabric_version}"
        //localRuntime "curse.maven:letsdo_wilder-1071657:${letsdo_wilder_fabric_version}"
    }
}

loom {
    def aw = project(":Common").file("src/main/resources/${mod_id}.accesswidener")
    if (aw.exists()) {
        accessWidenerPath.set(aw)
    }
    runs {
        client {
            client()
            setConfigName("Fabric Client")
            ideConfigGenerated(true)
            runDir("run")
        }
        server {
            server()
            setConfigName("Fabric Server")
            ideConfigGenerated(true)
            runDir("run")
        }
        commonDatagen {
            client()
            vmArg "-D${mod_id}.common_datagen=1"
            vmArg "-Dfabric-api.datagen"
            vmArg "-Dfabric-api.datagen.modid=${mod_id}"
            vmArg "-Dfabric-api.datagen.output-dir=${file("../Common/src/generated/resources")}"
        }
        fabricDatagen {
            inherit client
            vmArg "-Dfabric-api.datagen"
            vmArg "-Dfabric-api.datagen.modid=${mod_id}"
            vmArg "-Dfabric-api.datagen.output-dir=${file("src/generated/resources")}"
        }
    }
}

sourceSets.main.resources {
    srcDir 'src/generated/resources'
}

def changelogFile = project(":").projectDir.toString() + '/build/libs/changelog.txt'
def publishFile = project(":").projectDir.toString() + "/Fabric/build/libs/${identifier}-${version}.jar"
def display_name = "${project.name} ${minecraft_version}-${version}"
def display_version = "${minecraft_version}-${version}"

publishMods {
    file = file(publishFile)
    if (file(changelogFile).exists()) {
        changelog = file(changelogFile).text
    }
    displayName = "${display_name}"
    version = "${display_version}"
    type = "${release_type}".startsWith("release") ? STABLE : ("${release_type}".startsWith("beta") ? BETA : ALPHA)
    modLoaders.add('fabric')
    dryRun = providers.environmentVariable('DO_RELEASE').getOrNull() == null

    if (project.hasProperty("${curseforge_api_key_prop}")) {
        curseforge {
            accessToken = project.property("${curseforge_api_key_prop}")
            projectId = "${curseforge_id}"
            projectSlug = "${curseforge_slug}"
            javaVersions.add(JavaVersion.toVersion("${java_version}"))
            clientRequired = true
            serverRequired = true
            minecraftVersions.addAll("${release_versions}".split(','))
            requires('fabric-api')
            optional('actually-harvest')
            optional('betterdays')
            optional('charged-charms')
            optional('creeper-fireworks')
            optional('homeostatic-seasons')
            optional('mobchampions')
            optional('ready-player-fun')
            optional('simpletextoverlay')
            optional('survivalist-essentials')
            optional('jei')
            optional('serene-seasons')
            optional('biomes-o-plenty')
            optional('oh-the-biomes-weve-gone')
            optional('croptopia')
            optional('farmers-delight-refabricated')
            optional('xercamod')
            optional('kitteh6660s-morecraft')
            optional('sophisticated-backpacks')
            optional('ecologics')
            optional('sprout')
            optional('create-cafe')
            optional('fruit-trees')
            optional('leave-my-bars-alone')
            optional('create')
            optional('primal-winter')
            optional('more-food-xd')
            optional('eternal-starlight')
            optional('terralith')
            optional('regions-unexplored')
            optional('lets-do-bakery-farm-charm-compat')
            optional('lets-do-brewery-farm-charm-compat')
            optional('lets-do-candlelight-farm-charm-compat')
            optional('lets-do-herbal-brews')
            optional('lets-do-meadow')
            optional('lets-do-farm-charm')
            optional('lets-do-vinery')
            optional('lets-do-wildernature')
        }
    }
    if (project.hasProperty("${modrinth_api_key_prop}")) {
        modrinth {
            accessToken = project.property("${modrinth_api_key_prop}")
            projectId = "${modrinth_id}"
            minecraftVersions.addAll("${release_versions}".split(','))
            requires('fabric-api')
            optional('actually-harvest')
            optional('betterdays')
            optional('charged-charms')
            optional('creeper-fireworks-mod')
            optional('homeostatic-seasons')
            optional('mobchampions')
            optional('ready-player-fun')
            optional('simpletextoverlay')
            optional('survivalist-essentials')
            optional('create')
            optional('ecologics')
            optional('create-cafe')
            optional('scuba-gear')
            optional('farmers-delight-refabricated')
            optional('xercamod')
            optional('leave-my-bars-alone')
            optional('sprout')
            optional('jei')
            optional('oh-the-biomes-weve-gone')
            optional('fruitful-fun')
            optional('more-food-xd')
            optional('eternal-starlight')
            optional('terralith')
            optional('regions-unexplored')
            optional('lets-do-bakery-farmcharm-compat')
            optional('lets-do-brewery-farmcharm-compat')
            optional('lets-do-candlelight-farmcharm-compat')
            optional('lets-do-herbalbrews')
            optional('lets-do-meadow')
            optional('lets-do-farm-charm')
            optional('lets-do-vinery')
            optional('lets-do-wildernature')
        }
    }
    if (project.hasProperty("${discord_webhook_prop}")) {
        discord {
            webhookUrl = project.property("${discord_webhook_prop}")
            dryRunWebhookUrl = project.property("${discord_dryrun_webhook_prop}")
            username = "${mod_author}"
            content = changelog.map {
                "# A new version of ${mod_display_name} for Fabric ${minecraft_version} has been released! \n" + it
            }
        }
    }
}

publishing {
    publications {
        mavenJava(MavenPublication) {
            groupId project.group
            artifactId "${mod_id}-${minecraft_version}-fabric"
            version project.version
            from components.java
        }
    }
}
